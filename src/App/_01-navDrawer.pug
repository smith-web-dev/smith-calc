v-navigation-drawer(v-model='drawer' fixed clipped app)
  v-toolbar(flat)
    v-list.pa-0
      v-list-tile(avatar)
        v-list-tile-avatar
          img(src="http://smithindustrial.com/webapp/img/icon1.png")
        v-list-tile-content
          v-list-tile-title SmithCalc
          v-list-tile-sub-title v1.0.0
  //- v-list(dense)
  v-list
    //-
    v-list-tile(v-for='(n, i) in navItems' :key='i' :to='n.path')
      v-list-tile-action
        v-icon(v-if='n.icon.type === `mdi`') {{ n.icon.name }}
        sirp-icon(v-if='n.icon.type === `sirp`' width='24' height='24' :name='n.icon.name')
      v-list-tile-content
        v-list-tile-title {{ n.text }}
