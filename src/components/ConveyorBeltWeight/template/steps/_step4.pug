extends /layouts/_stepper-step-main.pug
block title
  | Results
block card-body
  v-list(
    three-line
    subheader)
    //- Part size x length
    v-list-tile
      v-list-tile-content
        v-list-tile-title Description
        v-list-tile-sub-title {{ beltDescription }}
      v-list-tile-action
        +copyBtn(
          click='doCopy(beltDescription)')
    //- Weight per piece
    v-list-tile
      v-list-tile-content
        v-list-tile-title Belt weight
        v-list-tile-sub-title {{ commaNum(beltWeight) }} pounds
      v-list-tile-action
        +copyBtn(
          click='doCopy((beltWeight + ` pounds`))')
    //- Extended weight
    v-list-tile
      v-list-tile-content
        v-list-tile-title Weight per foot
        v-list-tile-sub-title {{ commaNum(perFootWeight) }} pounds
      v-list-tile-action
        +copyBtn(
          click='doCopy((perFootWeight + ` pounds`))')
block nav-btns-block
  +resetAllBtn
