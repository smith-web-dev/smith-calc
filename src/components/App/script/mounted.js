// let pjson = require('../../../../package.json')

export function mounted () {
  // //
  // // Version check
  // //
  // let userVersion = (this.$ls.get('SmithCalcVersion'))
  // let calcVersion = pjson.version
  // console.log(userVersion)
  // if (userVersion !== calcVersion) {
  //   alert('old version')
  // } else {
  //   alert('current version')
  // }
}
