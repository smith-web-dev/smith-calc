extends ../../../../views/layouts/_stepper-step-main.pug
include ../../../../views/mixins/_index.pug
block title
  | Pulley diameters
block subtitle
  | Enter the diameters of the pulleys in {{ theAppIsMetric ? `centimeters` : `inches` }}.
block card-body
  v-layout(row)
    v-text-field(:color='fieldColor' type='number' :label='calcInput.equalPulleys ? `Pulley diameters (` + metricConvertCmIn + `)` : `First pulley diameter (` + metricConvertCmIn + `)`' box v-model='calcInput.pully.one' clearable)
  v-layout(row)
    v-text-field(:color='fieldColor' type='number' v-show='!calcInput.equalPulleys' :label='`Second pulley diameter (` + metricConvertCmIn + `)`' box v-model='calcInput.pully.two' clearable)
  v-layout(row)
    v-checkbox(label='Pulleys have equal diameters' v-model='calcInput.equalPulleys')
block nav-btns-right
  +stepBtnNext(click='moveStep(`f`)')
