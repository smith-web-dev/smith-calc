extends ../layouts/_stepper-step-main.pug
block title
  | Determine the average OD of the fitting shank
block subtitle
  | Measure the shank's outside diameter in at least 3 places, always at the highest points of the serrations. Input in {{ theAppIsMetric ? `centimeters` : `inches` }}
block card-body
  div(v-for='(f, i) in calcInput.shankMeas' :key='i')
    v-text-field(color='accent' type='number' label='' box v-model='calcInput.shankMeas[i]' clearable :label='`Shank OD #` + (i + 1) + [theAppIsMetric ? ` (cm)` : ` (in)`]')
      v-btn(flat icon color='error' slot='append-outer' v-show='(i + 1) > 3' style='top: -10px;' @click.native='removeMeas(calcInput.shankMeas, i)')
        v-icon delete
  v-layout(row justify-center)
    v-flex(xs12 sm10 md8)
      v-btn(block color='accent' @click.native='addMeasurement(calcInput.shankMeas)' :disabled='calcInput.shankMeas.length < 6 ? false : true') Add A Measurement
block nav-btns-left
  include ../Global/btns/_step-btn-back.pug
block nav-btns-right
  include ../Global/btns/_step-btn-next.pug
